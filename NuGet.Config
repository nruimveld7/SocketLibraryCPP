<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <packageSources>
    <!-- Relative to this config file (repo root) -->
    <add key="LocalBuild" value="build" />
    <!-- Keep nuget.org as a fallback if you publish later -->
    <add key="nuget.org" value="https://api.nuget.org/v3/index.json" />
  </packageSources>
</configuration>
